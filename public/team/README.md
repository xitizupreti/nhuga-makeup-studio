# Portrait

A photo of Oshika for the About page.

```
public/team/oshika.jpg
```

Formats: `.svg` `.png` `.webp` `.jpg` `.jpeg` `.avif`

Cropped to a **4:5 portrait** on the page, so a vertical photo works best.
Without one, the page shows the logo on a pink panel instead.

Read at build time — redeploy after adding.
